const serviceName = 'partner-mcp-gateway';

function main(): void {
  console.log(`${serviceName} ready`);
  // TODO: README "Tasks" 1 - add declarative routing table for path/method to upstream pool.
  // TODO: README "Tasks" 1 - wire request dispatch into the routing table.
}

main();